# 拼图接口商业化设计规范 V1

> 状态：几何样稿评审版，尚未经过实物打印冻结  
> 目标工艺：消费级 FDM、0.4 mm 喷嘴、PLA 为首轮基准  
> 配套样稿：`拼图接口样稿-v1.svg`

## 1. 产品目标

生成器保留三种拼接风格：

1. **经典柔和型**：默认风格，面向成人经典拼图。
2. **稳固互锁型**：适合需要移动局部拼图片段的场景，但不是卡扣。
3. **儿童大圆角型**：面向低片数、大拼片，优先保证宽颈、圆角和耐用性。

三种风格必须共用同一套共享边建模、间隙补偿、校验与导出流程。风格只改变曲线参数，不得修改成品尺寸、拼片厚度、打印间隙和用户图片。

## 2. 术语

- **共享边中心线** `C(t)`：相邻两块拼片共同使用的名义裁切线。
- **凸榫 / Tab**：共享边朝当前拼片外侧凸出的部分。
- **凹口 / Blank**：同一共享边在相邻拼片上的反向表现。
- **总间隙** `G`：两块实物拼片装配后的设计间隙。
- **边长** `E`：两个拼片交点之间的名义边长。
- **局部短边** `S`：当前单元格宽度与高度的较小值。

## 3. 不可妥协的几何规则

### 3.1 一条共享边只生成一次

每条内部边只生成一条 `C(t)`。相邻拼片分别正向和反向引用，不允许分别生成“凸榫”和“凹口”。共享边的理论互补误差应为零。

### 3.2 使用连续样条，不拼接圆形

单个接口由以下控制区域组成：

`直边 → 上肩 → 上颈 → 上腹 → 顶部 → 下腹 → 下颈 → 下肩 → 直边`

- 各段至少保持一阶切线连续（C1）；视觉主轮廓优先达到二阶曲率连续（C2）。
- 接口进入和离开直边时切线必须与直边一致。
- 禁止标准圆形直接连接直边、尖角、折线、局部回折和曲线自交。

### 3.3 间隙采用法向偏移

总间隙为 `G` 时，两块拼片分别沿实体内部方向偏移 `G / 2`。

- 禁止围绕拼片中心整体缩放轮廓。
- 偏移连接采用圆角连接，禁止产生尖锐 miter。
- 偏移后必须重新检查自交、最小颈宽和有效面积。
- 首层象脚补偿属于切片参数，不得混入接口风格参数。

FDM 装配不存在通用公差。首轮校准以 `0.20 / 0.25 / 0.30 / 0.35 / 0.40 mm` 总间隙生成测试条，由用户选择机器与材料对应档位。Prusa 的官方设计建议也明确指出公差受尺寸、方向、几何、校准和材料影响，并将约 `0.30 mm` 作为可动件的起始参考：<https://help.prusa3d.com/article/modeling-with-3d-printing-in-mind_164135>

## 4. 三套风格候选参数

以下是 V1 样稿范围，不是已冻结的生产参数。必须经过实物测试后才能标记为正式默认值。

| 参数 | 经典柔和型 | 稳固互锁型 | 儿童大圆角型 |
|---|---:|---:|---:|
| 名义深度 `D / S` | 0.18 | 0.22 | 0.14 |
| 深度随机范围 | 0.16–0.20 | 0.20–0.24 | 0.12–0.16 |
| 颈部宽度 `N / E` | 0.24 | 0.22 | 0.30 |
| 腹部宽度 `H / E` | 0.36 | 0.38 | 0.40 |
| 肩部总跨度 `A / E` | 0.62 | 0.66 | 0.68 |
| 中心位置扰动 | ±0.050 E | ±0.030 E | ±0.020 E |
| 左右不对称扰动 | ±0.035 E | ±0.025 E | ±0.020 E |
| 根部最小圆角 | 1.2 mm | 1.4 mm | 2.0 mm |
| 设计感受 | 顺滑、自然 | 更明显的保持力 | 宽颈、浅咬合、耐用 |

共同限制：

- 接口控制区域距离边角不得小于 `0.14 E`。
- 接口深度不得大于 `min(0.26 S, 0.24 E)`。
- 偏移后的有效颈宽不得小于四条0.4 mm挤出线宽，并且不得小于对应风格的实物测试下限。
- 单片不得因为接口生成而出现低于主体平均厚度35%的平面细腰。

## 5. 随机化规则

随机化用于避免假拼和明显重复，但不得破坏风格识别。

- 可随机：中心位置、深度、腹部宽度、左右不对称、凸凹方向。
- 同一风格内部至少包含宽圆、标准、细长三种受控轮廓族；不得只对同一模板做肉眼不可辨的轻微扰动。
- 不可随机：共享边连续性、总间隙、最小圆角、最小颈宽、成品外框尺寸。
- 不强制每块拼片同时具有凸榫和凹口。
- 全局凸榫与凹口数量应接近平衡，但允许局部连续方向。
- 相邻两块拼片不得拥有完全相同的量化轮廓签名。
- 相同随机种子、尺寸、行列数和风格必须生成完全一致的结果。

## 6. 用户界面规则

“拼接风格”和“装配松紧”是两个独立控件。

### 拼接风格

- 经典柔和型（默认）
- 稳固互锁型
- 儿童大圆角型

切换风格时保持：图片、宽度、高度、行列、厚度、总间隙和随机种子。

### 装配松紧

首轮提供：

- 紧密：0.20 mm（必须提示先打印校准条）
- 标准：0.30 mm
- 宽松：0.40 mm
- 自定义：0.00–0.80 mm

预设只能写入总间隙，不得改变曲线形状。

## 7. 儿童风格的商业合规边界

“儿童大圆角型”在生成器中只表示宽颈、浅咬合和大圆角的几何风格，不代表产品已经取得儿童玩具安全认证。

- 在没有完成年龄分级、拼片最小尺寸、机械与物理性能、材料和化学安全测试前，界面不得使用“儿童安全”“防吞咽”或类似承诺。
- 如产品面向儿童销售，必须根据销售地区完成独立合规评估；生成器的几何检查不能替代实验室检测。
- 截至2026年7月31日，中国现行强制标准包括 `GB 6675.1-2014` 和 `GB 6675.2-2014`；已发布的 `GB 6675.1-2025`、`GB 6675.2-2025` 将于2026年11月1日实施，商业计划必须覆盖标准切换。
- 美国市场还需评估 CPSC 小部件法规及强制玩具安全标准；不同年龄组要求不能通过单一“最小拼片尺寸”参数概括。

官方信息：

- 中国 `GB 6675.1-2025`：<https://openstd.samr.gov.cn/bzgk/std/newGbInfo?hcno=C9E23F1F4C5C26B9AB4EF697C8F38E63>
- 中国现行 `GB 6675.2-2014`：<https://std.samr.gov.cn/gb/search/gbDetailedCNF?id=71F772D80145D3A7E05397BE0A0AB82A>
- 美国 CPSC 小部件法规摘要：<https://www.cpsc.gov/s3fs-public/Small-Parts-16-C-F-R-Part-1501-English.pdf>

## 8. 自动几何验收

每次生成和导出前必须通过：

1. 共享边互补误差检查。
2. 闭合轮廓与方向检查。
3. 曲线和偏移轮廓自交检查。
4. 最小颈宽、最小圆角、最小面积检查。
5. 三角剖分和 STL 流形检查。
6. 成品外框尺寸检查。
7. 拼片间重叠检查。
8. 打印板越界检查。

数字压力测试最低要求：

- 每种风格至少10,000个随机种子。
- 覆盖2×2至10×8的全部行列组合。
- 覆盖最小、默认和最大成品尺寸。
- 几何失败率必须为0。
- 同种子重复生成结果必须一致。

## 9. 实物测试门槛

正式发布前至少完成：

1. 五档间隙校准条打印。
2. 三种风格各打印一套5×4完整拼图。
3. PLA 与 PETG 各完成一轮装配。
4. 至少覆盖一台床甩式和一台 CoreXY 设备。
5. 每套重复装拆30次，检查白化、裂纹、永久变形和间隙增长。
6. 记录插入力、拔出力和局部拼片组移动能力；首轮数据用于冻结正式阈值，不预设未经测试的力值。
7. 首层象脚补偿开启和关闭各验证一次。

## 10. 阶段门禁

- **G0 视觉确认**：确认本规范与三套1:1 SVG 样稿。
- **G1 几何引擎**：实现共享样条和法向偏移。
- **G2 数字验证**：完成全组合压力测试。
- **G3 实物校准**：打印间隙条和三套样品，冻结参数。
- **G4 产品接入**：加入风格选择、说明和导出命名。

在 G3 完成前，任何曲线比例和默认间隙都必须标记为“候选”，不得宣称已达到量产标准。

## 11. 设计依据

- 成熟经典拼图强调精密、连续的互锁裁切，而不是圆形卡扣：<https://www.ravensburger.us/en-US/products/jigsaw-puzzles/puzzles-for-adults/the-shell-collector-17321>
- 经典网格、随机裁切是两种不同产品语言：<https://www.cobblehillpuzzles.com/pages/our-puzzles>
- 公共边可通过控制点与样条统一生成，并对控制点施加受约束随机：<https://patents.justia.com/patent/20030045335>
- 传统刀模公开结构使用连续的 S 形凹曲线，并允许变化刀模尺寸与方向：<https://patents.google.com/patent/US3398614A/en>
